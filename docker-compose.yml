services:
  wedding-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: erza-prili-wedding
    ports:
      - "8081:80"
    restart: unless-stopped
    labels:
      - "com.docker.compose.project=wedding-invitation"
